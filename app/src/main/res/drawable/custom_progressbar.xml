<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/progress_back"
                android:centerColor="@color/progress_back"
                android:centerY="1.0"
                android:endColor="@color/progress_back"
                android:angle="270"
                />

            <corners android:radius="20dp"/>
        </shape>

    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/progress_front"
                    android:centerColor="@color/progress_front"
                    android:centerY="1.0"
                    android:endColor="@color/progress_front"
                    android:angle="270"
                    />
                <corners android:radius="20dp"/>
            </shape>
        </clip>
    </item>
</layer-list>